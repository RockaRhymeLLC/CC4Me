{
  "id": "t-019",
  "title": "Auth rejects invalid bearer token",
  "description": "Verify that requests without valid auth are rejected",
  "storyRefs": ["s-n01"],
  "planRef": "plans/20260203-agent-to-agent-comms.plan.md",
  "type": "story",
  "status": "passed",
  "steps": [
    {
      "order": 1,
      "action": "POST /agent/message with no Authorization header",
      "expected": "Returns 401 with error message"
    },
    {
      "order": 2,
      "action": "POST /agent/message with wrong bearer token",
      "expected": "Returns 401 with error message"
    },
    {
      "order": 3,
      "action": "POST /agent/message with correct bearer token from Keychain",
      "expected": "Returns 200 (auth passes, may fail on body validation separately)"
    }
  ],
  "created": "2026-02-03T22:30:00Z",
  "executedAt": "2026-02-04T00:15:00Z",
  "result": "All 3 steps pass. 401 for no/wrong token, 200 with ok:true for correct token."
}
